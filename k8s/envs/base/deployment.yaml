apiVersion: apps/v1
kind: Deployment
metadata:
  name: classificateur-ia
  labels:
    app: classificateur-ia
spec:
  replicas: 1
  selector:
    matchLabels:
      app: classificateur-ia
  template:
    metadata:
      labels:
        app: classificateur-ia
    spec:
      containers:
      - name: classificateur-ia
        image: rom55555/classificateur-ia:v1.0.0
        ports:
        - containerPort: 8501
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 8501
          initialDelaySeconds: 90
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /
            port: 8501
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 10
          failureThreshold: 3
        env:
        - name: ENVIRONMENT
          value: "base"
