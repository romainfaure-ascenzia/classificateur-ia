apiVersion: apps/v1
kind: Deployment
metadata:
  name: classificateur-ia
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: classificateur-ia
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        env:
        - name: ENVIRONMENT
          value: "production"
